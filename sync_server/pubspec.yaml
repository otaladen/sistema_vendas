name: sistema_vendas_sync_server
description: Servidor HTTP de sincronizacao LAN para sistema_vendas.
publish_to: 'none'

environment:
  sdk: ^3.11.5

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.9.4
