import 'dart:typed_data';

Future<void> enviarRawImpressoraWindows(String printerName, Uint8List data) async {
  throw UnsupportedError(
    'Envio RAW para gaveta disponivel apenas no Windows desktop.',
  );
}
